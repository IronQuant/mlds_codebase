# Direct dependencies only. Versions are the ones the reported results were
# produced with; transitive packages are left to the resolver.
#
# torch is deliberately unpinned to a build: pip gives a CPU wheel on Windows and
# a CUDA wheel on Colab, and the encoder work needs the GPU one.

anthropic==0.117.0        # zero-shot LLM classification
fastexcel==0.20.2         # polars.read_excel backend for the benchmark splits
ftfy==6.3.1               # mojibake repair in corpus_helpers.normalize
gensim==4.4.0             # GoogleNews word2vec vectors
matplotlib==3.10.9        # figure notebooks + utils/plotting_helpers
numpy==2.4.6
pandas==3.0.3
polars==1.41.2
scikit-learn==1.9.0
sentencepiece==0.2.1      # tokenizer for the FLANG checkpoints
torch==2.12.0
transformers==5.10.2
